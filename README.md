# AI Brain Daily Digest

This folder is intended for GitHub Pages publication.

The published `index.html` is encrypted. It should not contain plaintext email
subjects or message bodies. Open the page and enter the Daily Digest web
password to decrypt the report locally in the browser.

Do not add raw AI_Brain files to this repository.
